{
  "name": "api_tests",
  "version": "1.0.0",
  "description": "testing Stage Demo",
	
  "scripts": {
    "api-tests": "newman run collection.json -e environment.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "^5.3.0"
  }
}

